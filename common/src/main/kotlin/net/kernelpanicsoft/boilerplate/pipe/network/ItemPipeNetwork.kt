package net.kernelpanicsoft.boilerplate.pipe.network

import java.util.UUID

/** A connected set of item-pipe positions - see [AbstractPipeNetwork]. */
class ItemPipeNetwork(id: UUID) : AbstractPipeNetwork(id)
