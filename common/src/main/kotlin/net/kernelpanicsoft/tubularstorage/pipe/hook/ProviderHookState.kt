package net.kernelpanicsoft.tubularstorage.pipe.hook

/** Self-contained state for one [ProviderHookType] attachment - purely a marker, no config of its own. */
class ProviderHookState : HookHolderState(ProviderHookType.ID)
