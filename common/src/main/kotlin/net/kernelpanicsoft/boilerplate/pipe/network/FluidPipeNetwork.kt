package net.kernelpanicsoft.boilerplate.pipe.network

import java.util.UUID

/** A connected set of fluid-pipe positions - see [AbstractPipeNetwork]. */
class FluidPipeNetwork(id: UUID) : AbstractPipeNetwork(id)
