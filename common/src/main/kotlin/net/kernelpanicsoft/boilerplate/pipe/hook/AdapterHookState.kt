package net.kernelpanicsoft.boilerplate.pipe.hook

/** No fields of its own - see [AdapterHookType]; the hook is purely a physical collar, nothing to persist beyond which face it's on. */
class AdapterHookState : HookHolderState(AdapterHookType.ID)
