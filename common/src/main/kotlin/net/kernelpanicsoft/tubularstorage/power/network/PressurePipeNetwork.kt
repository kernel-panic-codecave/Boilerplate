package net.kernelpanicsoft.tubularstorage.power.network

import net.kernelpanicsoft.tubularstorage.pipe.network.AbstractPipeNetwork
import java.util.UUID

/** A connected set of pressure-pipe positions - see [AbstractPipeNetwork]. */
class PressurePipeNetwork(id: UUID) : AbstractPipeNetwork(id)
